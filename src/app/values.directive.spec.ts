import { ValuesDirective } from './values.directive';

describe('ValuesDirective', () => {
  it('should create an instance', () => {
    const directive = new ValuesDirective();
    expect(directive).toBeTruthy();
  });
});
